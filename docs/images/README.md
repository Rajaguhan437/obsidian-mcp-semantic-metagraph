# Figures

Drop the three rendered infographics here, with these exact filenames — the
root `README.md` references them by relative path:

| file | what it shows |
|---|---|
| `architecture.png` | pipeline: vault → chunking → two representations → max scoring → provenance |
| `benchmarks.png` | nDCG@10 by stratum, ranking metrics, provenance split |
| `engineering.png` | the seven fixes, test coverage, rejected alternatives, performance |

Keep them under ~1 MB each so the README stays quick to load. If a figure and
the prose ever disagree, the prose and `docs/BENCHMARKS.md` are authoritative —
they are regenerated from live runs.
